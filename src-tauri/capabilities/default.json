{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus opening external links in the system browser",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
